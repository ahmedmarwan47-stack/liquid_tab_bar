name: liquid_tab_bar
description: An iOS 26-style floating liquid-glass tab bar for Flutter with a deformable droplet, refraction, search, actions, badges, and scroll-aware folding.
version: 2.0.0
repository: https://github.com/ahmedmarwan47-stack/liquid_tab_bar
issue_tracker: https://github.com/ahmedmarwan47-stack/liquid_tab_bar/issues
topics:
  - navigation
  - tabs
  - glassmorphism
  - ios
  - ui

environment:
  sdk: ^3.6.0
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  shaders:
    - assets/shaders/nav_glass.frag
    - assets/shaders/droplet_glass.frag
